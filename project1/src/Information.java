package project1.src;

public interface Information {
    public void displayInfo();
    // interfaces is kind of textual contract 
    // interfaces can be with different methods, parameters, types
    // every class that implements the interface it should implement all the methods in it 

    // classs can implement more than one interface
    // but classes can only extend one other class 
}
